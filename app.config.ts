export default defineAppConfig({
    ui: {
        colors: {
            primary: 'indigo',
        },
        input: {
            defaultVariants: {
                variant: 'soft'
            }
        },
        select: {
            defaultVariants: {
                variant: 'soft'
            }
        },
        button: {
            defaultVariants: {
                variant: 'soft'
            }
        }
    }
})